<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Record_shop_frontend" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Record_shop_frontend" parent="Base.Theme.Record_shop_frontend" />

    <style name="search_view_theme" parent="Widget.AppCompat.SearchView" >
        <!-- Query Text Color -->
        <item name="android:textColorPrimary">@color/white</item>
        <!-- Hint Color -->
        <item name="android:textColorHint">@color/white</item>
        <!-- Icon Color -->
        <item name="android:tint">@color/white</item>

        <item name="android:closeIcon">@drawable/baseline_close_24_white</item>

        <item name="closeIconTint">@color/white</item>

        <item name="closeIconVisible" />

    </style>

</resources>